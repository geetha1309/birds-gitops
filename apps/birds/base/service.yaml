apiVersion: v1
kind: Service
metadata:
  name: birds-app
  labels:
    app: birds-app
spec:
  selector:
    app: birds-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP